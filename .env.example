# Example environment variables — do NOT commit real secrets here.
SUPABASE_URL=
SUPABASE_ANON_KEY=
# For Firebase, paste your JSON config object flattened or store in a secure place.
FIREBASE_CONFIG=
